{
  "name": "timtam-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test --reporter=list",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:local": "playwright test local-sanity.spec.ts",
    "test:local:headed": "playwright test local-sanity.spec.ts --headed",
    "test:local:debug": "playwright test local-sanity.spec.ts --debug",
    "test:local-only": "playwright test --grep @local",
    "test:aws-only": "playwright test --grep @aws"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3",
    "@aws-sdk/client-dynamodb": "3.600.0",
    "@aws-sdk/lib-dynamodb": "3.600.0"
  }
}
